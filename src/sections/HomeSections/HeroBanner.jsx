import React from 'react';
import HeroSlider from '../../components/HeroSlider';

const HeroBanner = () => {
    return (
        <section>
            <HeroSlider />
        </section>
    );
};

export default HeroBanner;